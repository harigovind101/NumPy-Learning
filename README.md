# NumPy Learning Journey

My NumPy learning journey for building a strong AI/ML foundation.

## Progress

### Day 1: NumPy Array Basics ✅

Topics learned:

- Creating arrays using `np.array()`
- Understanding 1D and 2D arrays
- `ndim`
- `shape`
- `size`
- `dtype`
- Rows and columns
- Using `shape[0]` and `shape[1]`
- Understanding samples and features in AI/ML datasets

### Day 2: Creating NumPy Arrays ✅

Topics learned:

- `np.zeros()`
- `np.ones()`
- Using `dtype=int`
- `np.arange()`
- `np.linspace()`
- `np.random.default_rng()`
- `rng.random()`
- `rng.integers()`
- Creating random 1D and 2D arrays
- Understanding `size=` when generating arrays
- Creating AI/ML-style datasets using samples and features

### Day 3: 1D Indexing and Slicing ✅

Topics learned:

- Positive indexing
- Negative indexing
- Finding the last element using `.size`
- Slicing with `array[start:stop]`
- Slicing with `array[:stop]`
- Slicing with `array[start:]`
- Using steps with `array[start:stop:step]`
- Selecting every second element using `array[::2]`
- Reversing an array using `array[::-1]`
- Modifying individual elements using indexing
- Modifying multiple elements using slicing
- Understanding NumPy views
- Creating independent arrays using `.copy()`
- Applying indexing and slicing to AI/ML prediction data

## Learning Goal

Build a strong NumPy foundation before moving on to:

**NumPy → Pandas → Matplotlib → Statistics → Machine Learning → Deep Learning**

## Tools

- Python
- NumPy
- VS Code
- Git
- GitHub

## Repository Structure

- `01ArrayBasics.py` — NumPy array fundamentals
- `02ArrayCreation.py` — Creating NumPy arrays
- `03IndexingSlicing.py` — 1D indexing and slicing

## Current Status

Completed **Day 3: 1D Indexing and Slicing**.

Next topic: **Day 4 — 2D Indexing and Slicing**.